---
- name: Uruchom ipconfig na Windows przez SSH
  hosts: windows_ssh
  gather_facts: no

  tasks:
    - name: Uruchom PowerShell script
      shell: |
        powershell.exe -Command "ipconfig /all"
      register: output

    - name: Wyświetl wynik
      debug:
        var: output.stdout
